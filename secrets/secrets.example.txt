# ============================================================================
# YANN OS — Fichier central de TOUS les secrets du projet
# ============================================================================
#
# Comment l'utiliser :
#   1. Copie ce fichier vers `secrets/.env.all` (le vrai fichier, gitignored)
#   2. Remplis avec tes vraies valeurs
#   3. Quand tu rotates un secret : édite cette ligne
#   4. Run `bash scripts/sync-secrets.sh` → propage partout automatiquement
#
# IMPORTANT :
#   - `secrets/.env.all` est dans .gitignore — JAMAIS commité
#   - Ne colle JAMAIS le contenu de ce fichier (ni d'une commande qui le lit)
#     dans un chat / Slack / email / ticket
#   - Pour rotater UN secret : édite ici → run le script. C'est tout.
#
# Chaque secret a un commentaire `# WHERE:` qui liste TOUS les endroits où
# il est utilisé, et où le script va le pousser.
# ============================================================================


# --- Frontend / Supabase ---------------------------------------------------
# WHERE: .env (Vite build), VPS /root/dashboard-os/.env, GH Actions secrets
VITE_APP_NAME=YANN OS
VITE_SUPABASE_URL=https://flqjaqocsugwbkbiuhhk.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# --- Anthropic (chat agents V0.8 — utilisé par les edge functions) --------
# WHERE: Supabase Edge Functions secrets
DASHBOARD_AGENTS_ANTHROPIC_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# --- n8n webhook (partagé entre Caddy, edge functions, GH Actions) --------
# WHERE: Supabase Edge Functions secrets, VPS /root/dashboard-os/.env, GH Actions secrets
N8N_WEBHOOK_URL=https://n8n.makeitapp.fr/webhook/m01-trigger
N8N_WEBHOOK_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# --- n8n API key (pour gérer les workflows via REST API) ------------------
# WHERE: .env local (utilisé par les scripts Python patch_m01_v0.6.py, create_rerender.py, etc.)
N8N_API_KEY=eyJxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# --- Cloudflare (DNS coliver.re, etc.) ------------------------------------
# WHERE: .env local (utilisé par toi pour gérer les DNS)
CLOUDFLARE_API_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# --- cPanel O2Switch (legacy, pour les apps qui restent là-bas) -----------
# WHERE: .env local
CPANEL_HOST=xxx.o2switch.net
CPANEL_USER=xxxxxxxx
CPANEL_PASS=xxxxxxxx
CPANEL_API_TOKEN=xxxxxxxx


# --- VPS Hostinger (pour SSH depuis scripts locaux) -----------------------
# WHERE: utilisé par scripts/sync-secrets.sh pour push vers le VPS
VPS_HOST=2.24.8.83
VPS_USER=root
VPS_SSH_KEY_PATH=~/.ssh/hostinger_vps
